interface:
  display_name: "Klipper Ops"
  short_description: "Safe Klipper host ops through typed MCP tools and portable shell fallbacks."
  default_prompt: "Use klipper-ops MCP tools first for compact status, bounded logs, and staged config operations; require explicit confirmation before writes."
